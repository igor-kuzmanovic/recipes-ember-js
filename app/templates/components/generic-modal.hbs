{{#bs-modal open=isOpen onHidden=(action "onHidden") as |modal|}}
  {{#modal.header}}
    <h4 class="modal-title">{{title}}</h4>
  {{/modal.header}}
  {{#modal.body}}
    {{yield}}
  {{/modal.body}}
{{/bs-modal}}